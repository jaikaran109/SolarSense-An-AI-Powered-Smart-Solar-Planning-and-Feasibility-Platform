{
  "name": "solarsense-server",
  "version": "1.0.0",
  "description": "SolarSense Backend API - AI-Powered Smart Solar Planning and Feasibility Platform",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "node --test",
    "build": "npm --prefix ../client install && npm --prefix ../client run build",
    "render-build": "npm install && npm --prefix ../client install && npm --prefix ../client run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^8.7.0",
    "helmet": "^8.3.0",
    "mongoose": "^8.3.4"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
